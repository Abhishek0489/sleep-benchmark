<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= pageTitle %></title> <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-4xl font-bold"><%= pageTitle %></h1>
    <p class="text-lg mt-2">Total registered users: <strong><%= totalUsers %></strong></p>

    <div class="mt-8 bg-white p-6 rounded-lg shadow-md">
      <h2 class="text-2xl font-semibold">Monthly Logins</h2>
      <ul class="list-disc list-inside mt-4">
        <% dataPoints.forEach(point => { %>
          <li><%= point.month %>: <%= point.logins %> logins</li>
        <% }) %>
      </ul>
    </div>

    <a href="/dashboard" class="text-blue-600 mt-6 inline-block">← Back to Dashboard</a>
  </div>
</body>
</html>